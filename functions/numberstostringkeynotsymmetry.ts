/* 数字生成字符串键值 非对称式矩阵*/
export function numberstostringkeynotsymmetry(
    left: number,
    right: number
): `${number},${number}` {
    // let max = Math.max(left, right);
    // let min = Math.min(left, right);
    return `${left},${right}`;
}
