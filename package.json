{
  "name": "ant-colony-algorithm-function-test",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite -c vite.config.ts",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "npx prettier --write *.json *.ts *.html */*.ts */*.css *.md"
  },
  "devDependencies": {
    "typescript": "^4.5.5",
    "vite": "^2.8.4",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "mathjs": "^10.1.1"
  }
}
